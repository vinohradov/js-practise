/**
 * Created by ADMIN4IK on 31.05.2020.
 */
(function () {
    var month = 11;
    if ( month >= 1 && month <= 3) {
        console.log('зима');
    }
    if ( month >= 4 && month <= 6) {
        console.log('весна');
    }
    if ( month >= 7 && month <= 9) {
        console.log('лето');
    }
    if ( month >= 10 && month <= 12) {
        console.log('осень');
    }
})();